<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🌌 NoxPanel — AI Command Center</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/themes.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        
        .dashboard-card {
            background-color: var(--bg-card);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 20px;
            transition: var(--theme-transition);
            box-shadow: var(--shadow-sm);
        }
        
        .dashboard-card:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }
        
        .card-header {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
        }
        
        .card-icon {
            width: 40px;
            height: 40px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 12px;
            font-size: 1.2em;
        }
        
        .scripts-icon { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; }
        .ai-icon { background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); color: white; }
        .system-icon { background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%); color: white; }
        .logs-icon { background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%); color: white; }
        
        .stat-value {
            font-size: 2em;
            font-weight: bold;
            color: var(--text-primary);
            margin-bottom: 5px;
        }
        
        .stat-label {
            color: var(--text-muted);
            font-size: 0.9em;
        }
        
        .quick-actions {
            display: flex;
            gap: 10px;
            margin-top: 15px;
            flex-wrap: wrap;
        }
        
        .quick-action-btn {
            padding: 8px 16px;
            border-radius: 20px;
            border: 1px solid var(--border-color);
            background-color: var(--bg-secondary);
            color: var(--text-primary);
            text-decoration: none;
            font-size: 0.85em;
            transition: var(--theme-transition);
        }
        
        .quick-action-btn:hover {
            background-color: var(--bg-button);
            color: var(--text-inverse);
            transform: translateY(-1px);
        }
        
        .sidebar {
            background-color: var(--bg-sidebar);
            min-height: 100vh;
            padding: 20px 15px;
        }
        
        .nav-link {
            color: var(--text-inverse);
            border-radius: 8px;
            margin-bottom: 8px;
            padding: 12px 16px;
            transition: var(--theme-transition);
        }
        
        .nav-link:hover {
            background-color: var(--bg-tertiary);
            color: var(--text-inverse);
        }
        
        .nav-link.active {
            background-color: var(--bg-button);
            color: var(--text-inverse);
        }
        
        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 4px 12px;
            border-radius: 12px;
            font-size: 0.8em;
            font-weight: 500;
        }
        
        .status-online {
            background-color: rgba(34, 197, 94, 0.1);
            color: #22c55e;
            border: 1px solid rgba(34, 197, 94, 0.2);
        }
        
        .recent-activity {
            max-height: 300px;
            overflow-y: auto;
        }
        
        .activity-item {
            padding: 10px 0;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .activity-item:last-child {
            border-bottom: none;
        }
        
        .activity-icon {
            width: 30px;
            height: 30px;
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.8em;
            flex-shrink: 0;
        }
        
        .activity-success { background-color: rgba(34, 197, 94, 0.1); color: #22c55e; }
        .activity-info { background-color: rgba(59, 130, 246, 0.1); color: #3b82f6; }
        .activity-warning { background-color: rgba(245, 158, 11, 0.1); color: #f59e0b; }
        
        .activity-content {
            flex: 1;
        }
        
        .activity-title {
            font-weight: 500;
            margin-bottom: 2px;
            color: var(--text-primary);
        }
        
        .activity-time {
            font-size: 0.8em;
            color: var(--text-muted);
        }
    </style>
</head>
<body data-theme="light">
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <nav class="col-md-3 col-lg-2 d-md-block sidebar">
                <div class="sidebar-sticky pt-3">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h5 class="text-white mb-0">🌌 NoxPanel</h5>
                        <button id="theme-toggle" class="btn btn-sm btn-outline-light" title="Toggle theme">
                            <span class="theme-icon">🌓</span>
                        </button>
                    </div>
                    
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link active" href="/">
                                🏠 Dashboard
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/chat">
                                🤖 AI Assistant
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#" onclick="executeScript('system_diagnostic.py')">
                                🔍 System Diagnostic
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/api/models">
                                🧠 AI Models
                            </a>
                        </li>
                    </ul>
                    
                    <hr class="text-white-50">
                    <div class="text-white-50">
                        <small>System Status</small>
                        <div class="mt-2">
                            <span class="status-badge status-online">
                                <i class="fas fa-circle"></i>
                                Online
                            </span>
                        </div>
                    </div>
                </div>
            </nav>

            <!-- Main content -->
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2">Dashboard</h1>
                    <div class="btn-toolbar mb-2 mb-md-0">
                        <div class="btn-group me-2">
                            <button class="btn btn-sm btn-primary" onclick="refreshDashboard()">🔄 Refresh</button>
                            <button class="btn btn-sm btn-outline-secondary" onclick="showSystemInfo()">ℹ️ System Info</button>
                        </div>
                    </div>
                </div>

                <!-- Dashboard Grid -->
                <div class="dashboard-grid">
                    <!-- Scripts Card -->
                    <div class="dashboard-card">
                        <div class="card-header">
                            <div class="card-icon scripts-icon">
                                <i class="fas fa-code"></i>
                            </div>
                            <div>
                                <h5 class="mb-1">Script Library</h5>
                                <p class="mb-0 text-muted">Available automation scripts</p>
                            </div>
                        </div>
                        
                        <div class="stat-value" id="script-count">{{ scripts|length if scripts else 0 }}</div>
                        <div class="stat-label">Scripts Available</div>
                        
                        <div class="quick-actions">
                            {% if scripts %}
                                {% for script in scripts[:3] %}
                                <a href="#" class="quick-action-btn" onclick="runScript('{{ script }}')">
                                    {{ script.replace('.py', '').replace('_', ' ').title() }}
                                </a>
                                {% endfor %}
                                {% if scripts|length > 3 %}
                                <a href="#" class="quick-action-btn" onclick="showAllScripts()">View All</a>
                                {% endif %}
                            {% else %}
                                <span class="text-muted">No scripts found</span>
                            {% endif %}
                        </div>
                    </div>

                    <!-- AI Assistant Card -->
                    <div class="dashboard-card">
                        <div class="card-header">
                            <div class="card-icon ai-icon">
                                <i class="fas fa-robot"></i>
                            </div>
                            <div>
                                <h5 class="mb-1">AI Assistant</h5>
                                <p class="mb-0 text-muted">Intelligent automation helper</p>
                            </div>
                        </div>
                        
                        <div class="stat-value" id="ai-status">Ready</div>
                        <div class="stat-label">Assistant Status</div>
                        
                        <div class="quick-actions">
                            <a href="/chat" class="quick-action-btn">💬 Open Chat</a>
                            <a href="#" class="quick-action-btn" onclick="quickDiagnostic()">🔍 Quick Scan</a>
                            <a href="/api/models" class="quick-action-btn">🧠 Models</a>
                        </div>
                    </div>

                    <!-- System Metrics Card -->
                    <div class="dashboard-card">
                        <div class="card-header">
                            <div class="card-icon system-icon">
                                <i class="fas fa-server"></i>
                            </div>
                            <div>
                                <h5 class="mb-1">System Metrics</h5>
                                <p class="mb-0 text-muted">Real-time system status</p>
                            </div>
                        </div>
                        
                        <div class="row text-center">
                            <div class="col-4">
                                <div class="stat-value" style="font-size: 1.2em;" id="cpu-usage">--</div>
                                <div class="stat-label">CPU</div>
                            </div>
                            <div class="col-4">
                                <div class="stat-value" style="font-size: 1.2em;" id="memory-usage">--</div>
                                <div class="stat-label">Memory</div>
                            </div>
                            <div class="col-4">
                                <div class="stat-value" style="font-size: 1.2em;" id="disk-usage">--</div>
                                <div class="stat-label">Disk</div>
                            </div>
                        </div>
                        
                        <div class="quick-actions">
                            <a href="#" class="quick-action-btn" onclick="refreshMetrics()">🔄 Refresh</a>
                            <a href="#" class="quick-action-btn" onclick="executeScript('system_diagnostic.py')">📊 Full Report</a>
                        </div>
                    </div>

                    <!-- Recent Activity Card -->
                    <div class="dashboard-card">
                        <div class="card-header">
                            <div class="card-icon logs-icon">
                                <i class="fas fa-history"></i>
                            </div>
                            <div>
                                <h5 class="mb-1">Recent Activity</h5>
                                <p class="mb-0 text-muted">Latest system events</p>
                            </div>
                        </div>
                        
                        <div class="recent-activity" id="recent-activity">
                            <div class="activity-item">
                                <div class="activity-icon activity-success">
                                    <i class="fas fa-check"></i>
                                </div>
                                <div class="activity-content">
                                    <div class="activity-title">System initialized</div>
                                    <div class="activity-time">Just now</div>
                                </div>
                            </div>
                            <div class="activity-item">
                                <div class="activity-icon activity-info">
                                    <i class="fas fa-info"></i>
                                </div>
                                <div class="activity-content">
                                    <div class="activity-title">AI providers detected</div>
                                    <div class="activity-time">1 minute ago</div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="quick-actions">
                            <a href="#" class="quick-action-btn" onclick="refreshActivity()">🔄 Refresh</a>
                            <a href="#" class="quick-action-btn" onclick="viewLogs()">📋 View Logs</a>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>
                                    <i class="fas fa-play"></i> Run
                                </button>
                                <button class="btn btn-secondary" onclick="viewScript('{{ script }}')">
                                    <i class="fas fa-eye"></i> View
                                </button>
                            </div>
                        </div>
                        {% endfor %}
                    {% else %}
                        <div class="empty-state">
                            <i class="fas fa-folder-open"></i>
                            <h3>No Scripts Found</h3>
                            <p>Add Python scripts to the /scripts directory to get started.</p>
                        </div>
                    {% endif %}
                </div>
            </div>

            <div id="logs-tab" class="tab-content">
                <div class="logs-container">
                    <div class="logs-header">
                        <h3><i class="fas fa-list-alt"></i> Execution Logs</h3>
                        <button class="btn btn-secondary" onclick="clearLogs()">
                            <i class="fas fa-trash"></i> Clear
                        </button>
                    </div>
                    <div id="logs-content" class="logs-content">
                        <div class="log-entry">
                            <span class="timestamp">Ready</span>
                            <span class="message">NoxPanel initialized successfully</span>
                        </div>
                    </div>
                </div>
            </div>

            <div id="system-tab" class="tab-content">
                <div class="system-info">
                    <div class="info-card">
                        <h3><i class="fas fa-server"></i> System Status</h3>
                        <div class="info-grid">
                            <div class="info-item">
                                <label>Status:</label>
                                <span class="value online">Online</span>
                            </div>
                            <div class="info-item">
                                <label>Version:</label>
                                <span class="value">1.0.0</span>
                            </div>
                            <div class="info-item">
                                <label>Uptime:</label>
                                <span class="value" id="uptime">--</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <div id="output-modal" class="modal">
            <div class="modal-content">
                <div class="modal-header">
                    <h3 id="modal-title">Script Output</h3>
                    <button class="modal-close" onclick="closeModal()">&times;</button>
                </div>
                <div class="modal-body">
                    <pre id="output-content"></pre>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Enhanced Dashboard JavaScript
        
        function runScript(scriptName) {
            fetch('/run-script', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ script: scriptName })
            })
            .then(response => response.json())
            .then(data => {
                if (data.status === 'success') {
                    addActivity(`Script executed: ${scriptName}`, 'success');
                    alert(`Script "${scriptName}" executed successfully!\n\nOutput:\n${data.output.substring(0, 500)}${data.output.length > 500 ? '...' : ''}`);
                } else {
                    addActivity(`Script failed: ${scriptName}`, 'warning');
                    alert(`Error running script "${scriptName}":\n${data.message}`);
                }
            })
            .catch(error => {
                addActivity(`Script error: ${scriptName}`, 'warning');
                alert(`Error: ${error.message}`);
            });
        }
        
        function executeScript(scriptName) {
            runScript(scriptName);
        }
        
        function quickDiagnostic() {
            runScript('system_diagnostic.py');
        }
        
        function refreshMetrics() {
            // Simulate fetching system metrics
            const cpuUsage = Math.floor(Math.random() * 100);
            const memoryUsage = Math.floor(Math.random() * 100);
            const diskUsage = Math.floor(Math.random() * 100);
            
            document.getElementById('cpu-usage').textContent = cpuUsage + '%';
            document.getElementById('memory-usage').textContent = memoryUsage + '%';
            document.getElementById('disk-usage').textContent = diskUsage + '%';
            
            addActivity('System metrics updated', 'info');
        }
        
        function refreshDashboard() {
            refreshMetrics();
            refreshActivity();
            checkAIStatus();
            addActivity('Dashboard refreshed', 'info');
        }
        
        function refreshActivity() {
            addActivity('Activity log refreshed', 'info');
        }
        
        function addActivity(message, type = 'info') {
            const activityContainer = document.getElementById('recent-activity');
            const activityItem = document.createElement('div');
            activityItem.className = 'activity-item';
            
            const iconClass = type === 'success' ? 'fas fa-check' : 
                             type === 'warning' ? 'fas fa-exclamation-triangle' : 
                             'fas fa-info';
            const iconBg = type === 'success' ? 'activity-success' : 
                          type === 'warning' ? 'activity-warning' : 
                          'activity-info';
            
            activityItem.innerHTML = `
                <div class="activity-icon ${iconBg}">
                    <i class="${iconClass}"></i>
                </div>
                <div class="activity-content">
                    <div class="activity-title">${message}</div>
                    <div class="activity-time">Just now</div>
                </div>
            `;
            
            activityContainer.insertBefore(activityItem, activityContainer.firstChild);
            
            // Keep only the latest 5 activities
            while (activityContainer.children.length > 5) {
                activityContainer.removeChild(activityContainer.lastChild);
            }
        }
        
        function checkAIStatus() {
            fetch('/api/chat/status')
                .then(response => response.json())
                .then(data => {
                    const statusElement = document.getElementById('ai-status');
                    if (data.status === 'success') {
                        statusElement.textContent = 'Ready';
                        statusElement.className = 'stat-value';
                    } else {
                        statusElement.textContent = 'Offline';
                        statusElement.className = 'stat-value text-warning';
                    }
                })
                .catch(error => {
                    const statusElement = document.getElementById('ai-status');
                    statusElement.textContent = 'Error';
                    statusElement.className = 'stat-value text-danger';
                });
        }
        
        function showSystemInfo() {
            alert('NoxPanel v7.0.0\nAI-Powered Automation Platform\n\nFeatures:\n• Script execution\n• AI assistant\n• System monitoring\n• Theme support');
        }
        
        function showAllScripts() {
            window.location.href = '/';
        }
        
        function viewLogs() {
            // Implementation for viewing logs
            alert('Log viewer not yet implemented');
        }
        
        // Initialize dashboard on load
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize theme system
            if (typeof ThemeManager !== 'undefined') {
                const themeManager = new ThemeManager();
                themeManager.init();
                
                const themeToggle = document.getElementById('theme-toggle');
                if (themeToggle) {
                    themeToggle.addEventListener('click', () => {
                        themeManager.toggleTheme();
                    });
                }
            } else {
                // Fallback theme toggle
                const themeToggle = document.getElementById('theme-toggle');
                if (themeToggle) {
                    themeToggle.addEventListener('click', function() {
                        const body = document.body;
                        const currentTheme = body.getAttribute('data-theme') || 'light';
                        const newTheme = currentTheme === 'light' ? 'dark' : 'light';
                        body.setAttribute('data-theme', newTheme);
                        localStorage.setItem('theme', newTheme);
                    });
                }
            }
            
            // Load saved theme
            const savedTheme = localStorage.getItem('theme') || 'light';
            document.body.setAttribute('data-theme', savedTheme);
            
            // Initial data load
            refreshMetrics();
            checkAIStatus();
            
            // Update metrics every 30 seconds
            setInterval(refreshMetrics, 30000);
            
            // Check AI status every 60 seconds
            setInterval(checkAIStatus, 60000);
        });
    </script>
    
    <!-- Include theme manager -->
    <script src="{{ url_for('static', filename='js/theme-manager.js') }}" onerror="console.log('Theme manager not available, using fallback')"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>